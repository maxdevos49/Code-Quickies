#include "lexer.h"
#include <stdio.h>
#include <stdlib.h>

void usage(void)
{
    fprintf(stderr, "OVERVIEW: Calculator\n\n");
    fprintf(stderr, "Usage: calc <expression> [options]\n\n");
    fprintf(stderr, "OPTIONS:\n");
    fprintf(stderr, "\t--repl         Open a repl for solving arithmetic expressions.\n");
}

int main(int argc, char *argv[]) {

  if()

  return EXIT_SUCCESS;
}
